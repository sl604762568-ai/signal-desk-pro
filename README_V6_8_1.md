# v6.8.1 稳定实时热修复

- 修复前端 `loadDashboard()` 被错误嵌套在 `renderAll()` 中，导致首页永久显示“连接真实行情”。
- 首页实时刷新改为 fast-live：先返回真实全A快照，不再等待连板历史、量比历史和候选股历史K线。
- 深度K线、缠论、板块成分等仍在对应模块按需计算。
- `/api/health` 版本：`6.8.1-stable-live`。
